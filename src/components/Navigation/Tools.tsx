import { FC } from 'react';

interface Props {}

const Layout: FC<Props> = () => {
    console.log('ccc');
    return (
        <a className="navbar-item">
            Tools
        </a>
    );
};

export default Layout;
